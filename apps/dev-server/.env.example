# Dev-server environment variables
# (LLM proxy has been moved to the learner app — configure in apps/learner/.env)

# OPEN_EDU_PACKAGE_DIR: absolute path to a course package/bundle directory to load
# OPEN_EDU_STUDIO_MODE: 'creator' (default) or 'developer' — override the studio mode
