# Interactive Engine Demo

This package demonstrates the `{ type: "interactive" }` node:

1. A **guided** number line — select the highlighted marker
2. A **practice** number line — tap the emphasized number
3. A **composed lesson** where timeline selection drives visual focus

Use **Mark complete** on each interactive step, then click **Next** to continue.
