---
'@open-edu/schemas': minor
'@open-edu/oep-distribution': minor
'@open-edu/registry': minor
---

Add RegistryMetadataSchema, enriched catalog fields, draft-7 JSON Schema generation, catalog checksum enforcement on install, and the new @open-edu/registry package (GitHub-native course registry tooling). All three packages are published as ESM-only (the `exports` map exposes an `import` condition only), and the published `dist` excludes test files.
